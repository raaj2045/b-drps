# See https://help.github.com/articles/ignoring-files/ for more about ignoring files.

# dependencies
/node_modules
/.pnp
.pnp.js

# testing
/coverage

# production
/build



# misc
.DS_Store
.env
.env.local
.env.development.local
.env.test.local
.env.production.local


npm-debug.log*
yarn-debug.log*
yarn-error.log*

# Hardhat
/artifacts
/cache
/typechain-types
/coverage.json
# Local deployment manifest is regenerated on every `npm run deploy:local`;
# only the public-testnet manifests (deployments/sepolia.json, etc.) are
# committed.
/deployments/localhost.json

# Security/analysis tool artifacts (regenerable; reproducer call sequences are
# documented in benchmarks/SECURITY_ANALYSIS.md). The benchmark reports, CSVs,
# figures, and raw slither/solhint output under benchmarks/ stay tracked.
/crytic-export
/benchmarks/echidna-corpus
/benchmarks/echidna-corpus-guarded
/benchmarks/.cache

# Local agent/editor tooling
.mcp.json
